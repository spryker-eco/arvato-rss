build:
    before:
        - composer config repositories.spryker git git@github.com:spryker/spryker.git
    environment:
        php:
            version: 7.1

checks:
    php:
        code_rating: true

filter:
    excluded_paths:
        - config/*
        - tests/*
        - src/Generated/*
        - src/Pyz/*